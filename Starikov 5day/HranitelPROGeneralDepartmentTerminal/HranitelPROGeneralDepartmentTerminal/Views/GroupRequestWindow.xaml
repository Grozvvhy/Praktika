<Window x:Class="HranitelPROGeneralDepartmentTerminal.Views.GroupRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Групповая заявка на посещение" Height="750" Width="950"
        WindowStartupLocation="CenterScreen" MinHeight="650" MinWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Данные заявки -->
            <GroupBox Header="Данные заявки" Grid.Row="0" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Цель:" VerticalAlignment="Center"/>
                    <TextBox x:Name="PurposeTextBox" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Подразделение:" Grid.Row="1" VerticalAlignment="Center"/>
                    <ComboBox x:Name="DepartmentComboBox" Grid.Column="1" Grid.Row="1" Margin="5" 
                              DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="DepartmentComboBox_SelectionChanged"/>

                    <TextBlock Text="Ответственный:" Grid.Row="2" VerticalAlignment="Center"/>
                    <ComboBox x:Name="EmployeeComboBox" Grid.Column="1" Grid.Row="2" Margin="5" 
                              DisplayMemberPath="FullName" SelectedValuePath="Id"/>

                    <TextBlock Text="Дата начала:" Grid.Row="3" VerticalAlignment="Center"/>
                    <DatePicker x:Name="StartDatePicker" Grid.Column="1" Grid.Row="3" Margin="5"/>

                    <TextBlock Text="Дата окончания:" Grid.Row="4" VerticalAlignment="Center"/>
                    <DatePicker x:Name="EndDatePicker" Grid.Column="1" Grid.Row="4" Margin="5"/>
                </Grid>
            </GroupBox>

            <!-- Список посетителей -->
            <GroupBox Header="Посетители" Grid.Row="1" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="AddVisitorButton" Content="Добавить посетителя" Click="AddVisitorButton_Click" 
                                Width="160" Height="25" Margin="5"/>
                    </StackPanel>
                    <DataGrid x:Name="VisitorsDataGrid" Grid.Row="1" AutoGenerateColumns="False" Height="220" FontSize="11">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="100"/>
                            <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                            <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="100"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="160"/>
                            <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                            <DataGridTextColumn Header="Организация" Binding="{Binding Organization}" Width="120"/>
                            <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Серия паспорта" Binding="{Binding PassportSeries}" Width="90"/>
                            <DataGridTextColumn Header="Номер паспорта" Binding="{Binding PassportNumber}" Width="90"/>
                            <DataGridTemplateColumn Width="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="X" Click="RemoveVisitor_Click" Tag="{Binding}" 
                                                Background="LightCoral" Width="20" Height="20"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <!-- Кнопки -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SubmitButton" Content="Отправить заявку" Click="SubmitButton_Click" 
                        Width="150" Height="30" Margin="5"/>
                <Button Content="Отмена" Click="CancelButton_Click" Width="100" Height="30" Margin="5"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>